export interface Dish {
    id: string;
    name: string;
    price: number;
    ingredients: string[];
}

export interface NormalizedDish {
    id: string;
    name: string;
    price: number;
    ingredients: string[];
}
